import 'package:flutter/material.dart';
import 'package:flutter_gen/gen_l10n/app_localizations.dart';
import 'package:audioplayers/audioplayers.dart';





const tempoSelectorValue = [
  30,60,80,100,120,150,180,240
];

const tempoMax = 360;
const tempoMin = 10;

const tempoSelectorColors = [
  Colors.green,
  Colors.lightGreen,
  Colors.greenAccent,
  Colors.blueGrey,
  Colors.blue,
  Colors.lightBlue,
  Colors.yellow,
  Colors.deepOrangeAccent
];

const beat1Color = Colors.green;
const beat2Color = Colors.lightGreen;
const rhythmColor = Colors.orangeAccent;
const toneColor = Colors.blueAccent;

const unselectedColor = Colors.grey;

const drawerTextColor = Color(0xFCFFFFFF);



const appbarTitleDrawerIcon = Icons.view_headline;

const appbarTitleBackIcon = Icons.arrow_back;

const appbarForegroundColor = Colors.black;

const appbarBackgroundColor = Colors.white;

const appbarTitleStyle = TextStyle(color: appbarForegroundColor, fontFamily: 'Georgia', fontSize: 16);






















